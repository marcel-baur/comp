fun fib(n) {
	print n;
    if (n < 2) {
		print "N retunr";
		return n; 
	}
	let nminusone = n - 1;
	let nminustwo = n - 2;
	let one = fib(nminusone);
	let two = fib(nminustwo);
	print n;
	print one;
	print two;
	print "-------------";
	return one + two;
}

let start = clock();
print fib(5);
print clock() - start;
